# Protect sensitive files in the admin directory

# Deny access to config files
<FilesMatch "(config\.php|\.json)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
  </IfModule>
</FilesMatch>
